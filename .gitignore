# Build
target/
dependency-reduced-pom.xml

# Empacotamento portavel (gerado pelo script de release; publicado como asset do GitHub Release)
jpackage-input/
release/
transcritor-ata-portable-win64-*.zip

# Eclipse
.project
.classpath
.settings/
bin/

# IntelliJ (por precaucao)
.idea/
*.iml

# Logs
logs/
*.log

# Modelos (grandes, nao versionar) -- mas NAO os modelos ONNX embutidos no jar (src/main/resources/models),
# que sao pequenos (~32 MB) e essenciais ao build.
*.bin
/models/
vosk-model*/
!src/main/resources/models/

# Ferramentas de terceiros baixadas localmente (LIUM_SpkDiarization, etc.)
tools/

# Arquivos temporarios
*.tmp
*.wav

# Configuracao local do usuario (pode conter chave de API)
config.properties

# SO
Thumbs.db
.DS_Store
