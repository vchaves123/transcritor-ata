package com.tailor.transcritorata.deps;

/**
 * The application's own version. Single source of truth: bump this when cutting a release (see
 * CONTRIBUTING.md), instead of the version string that used to live only in {@code AboutDialog}.
 */
public final class AppVersion {

    public static final String CURRENT = "1.0.7";

    private AppVersion() {
    }
}
